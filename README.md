
  # center square and header

  This is a code bundle for center square and header. The original project is available at https://www.figma.com/design/kE7vOyOST3EQ5My1jybMEO/center-square-and-header.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  